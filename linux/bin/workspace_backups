#!/bin/sh
cd $WORKSPACE
for d in * ; do
  cd $WORKSPACE/$d
  for sd in * ; do
    echo "backing up $sd"
    tar $GIT_BACKUPS_EXCLUDES -zcf "$GIT_BACKUPS_DESTINATION/$d/$sd.tar.gz" $sd;
    echo ""
  done;
done;
